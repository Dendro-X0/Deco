# v0.6.12 manifest — Native / Android follow-ups (draft)

**Status:** In progress  
**Target release:** `v0.6.12`  
**Theme:** Optional breadth after Xmake/Premake/Qt: Bazel global cache hints, Android/Gradle native intermediates — only with strong markers and review-tier defaults.

Parent: [v0.6.x-roadmap.md](v0.6.x-roadmap.md) · Follows [v0.6.11-manifest.md](v0.6.11-manifest.md).

---

## Candidate scope (pick in priority order)

| ID | Idea | Notes |
|----|------|--------|
| **B1** | Bazel `output_user_root` or well-known cache roots | Env / docs-gated; never guess arbitrary `bazel-*` outside workspace |
| **A1** | Android `.cxx` / CMake intermediates under Gradle tree | Requires `build.gradle` / `gradlew` markers; review by default |
| **P1** | Polish: inventory fingerprint + docs for new discovery flags | If any 0.6.11 gaps in Settings ↔ engine parity |

## Non-goals

- New `schema_version` unless wire shape or kinds change  
- Auto-delete or lowering review tier on global caches

## Verification

```bash
pnpm check
```
