# v0.8.0 manifest — Multi-platform installers

**Status:** Shipped (`v0.8.0`)  
**Target release:** `v0.8.0`  
**Theme:** Release desktop installers and CLI packages for **Windows, macOS, and Linux** from CI — completes Phase E “platform reach” foundation.

Parent: [v0.8.x-roadmap.md](v0.8.x-roadmap.md) · Follows [v0.7.8-manifest.md](v0.7.8-manifest.md).

---

## Problem

- Through **`v0.7.8`**, [release.yml](../../.github/workflows/release.yml) shipped **Windows-only** installers while CI tested on Ubuntu/macOS.
- Maintainer primary OS is Windows; **`v0.8.0`** adds macOS and Linux bundles on every tagged release.

## Scope

| ID | Deliverable | Status |
|----|-------------|--------|
| **R1** | **Icons & Tauri bundle targets** | Shipped — icons present; `dmg`, `deb`, `appimage` in `tauri.conf.json` |
| **R2** | **Release workflow matrix** | Shipped — `release.yml` build matrix on Windows, macOS, Linux |
| **R3** | **Per-OS CLI zips** | Shipped — `deco-cli-win-x64`, `deco-cli-macos-aarch64`, `deco-cli-linux-x64` + `deco` launcher |
| **R4** | **In-app updates** | Shipped — `pickPlatformDownloadAssets` + platform install hints updated |
| **D1** | **Docs** | Shipped — distribution docs + install guide |

## Verification

```bash
pnpm check
```

Manual: install smoke test on Windows, macOS, and Linux VMs or hardware per [milestone-8](../milestones/milestone-8.md) checklist (adapt per OS).

## References

- [ci-and-release-platforms.md](../distribution/ci-and-release-platforms.md)
