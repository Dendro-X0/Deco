# v0.8.1 manifest — winget & Homebrew

**Status:** Shipped (`v0.8.1`)  
**Target release:** `v0.8.1`  
**Theme:** Package-manager distribution for **Windows (winget)** and **macOS (Homebrew)** after `v0.8.0` native installers ship.

Parent: [v0.8.x-roadmap.md](v0.8.x-roadmap.md) · Follows [v0.8.0-manifest.md](v0.8.0-manifest.md).

---

## Scope

| ID | Deliverable | Status |
|----|-------------|--------|
| **W1** | **winget manifest** | Shipped — `packaging/winget/` + sync script; PR to microsoft/winget-pkgs per release |
| **H1** | **Homebrew cask** | Shipped — `packaging/homebrew/Casks/deco.rb` (install via raw URL or path) |
| **L1** | **Linux note** | Shipped — [package-managers.md](../distribution/package-managers.md#linux) |
| **D1** | **Docs** | Shipped — install, github-releases, release-process |

## Verification

Manual install via winget and `brew install` on clean machines; `pnpm check`.
