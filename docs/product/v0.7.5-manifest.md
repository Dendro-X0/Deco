# v0.7.5 manifest — Classification parity (round 2)

**Status:** In progress  
**Target release:** `v0.7.5`  
**Theme:** Expand shared classification fixtures so CLI and Rust stay aligned on **project kinds**, **path policy**, and additional **global caches** — one feature set per [version-roadmap](version-roadmap.md) Phase D.

Parent: [v0.7.x-roadmap.md](v0.7.x-roadmap.md) · Follows [v0.7.4-manifest.md](v0.7.4-manifest.md).

---

## Problem

- `v0.7.4` added rust / python-venv / go-global cases; most kinds and blocked-path rules still lack fixture coverage.
- Drift on JVM, .NET, IDE paths, and remaining globals is the highest remaining parity risk before Phase E.

## Scope

| ID | Deliverable | Status |
|----|-------------|--------|
| **P1** | **Expand `cases.json` (round 2)** | Done — JVM/.NET/Python/Go project kinds, Electron blocked path, three global caches |
| **P2** | **Parity tests stay green** | Done — Vitest + Rust `classification_parity` |
| **P3** | **Project root detection** | Done — JVM, .NET, Python markers in `detect_project_root` (TS + Rust) |
| **D1** | **Docs** | Done — [ci-automation.md](../cli/ci-automation.md), roadmaps |

## Non-goals

- Policy gallery UI (→ `v0.7.6`)  
- Workspace rollups (→ `v0.7.7`)  
- Multi-platform installers (→ `v0.8.0`)  
- Scan contract bump without [contract changelog](../contract/changelog.md)

## Verification

```bash
pnpm check
```

## References

- [classification/cases.json](../../tests/fixtures/classification/cases.json)  
- [status.md](status.md)
