# v0.7.3 manifest — Classification parity + CI automation (draft)

**Status:** In progress  
**Target release:** `v0.7.3`  
**Theme:** Close **Phase D** engineering gaps — shared classification fixtures (TS/Rust alignment) and a documented **CI scan recipe** (`deco --json`, exit semantics, optional reclaim gate). No marketing assets in this release.

Parent: [v0.7.x-roadmap.md](v0.7.x-roadmap.md) · Follows [v0.7.2-manifest.md](v0.7.2-manifest.md).

---

## Problem

- Policy packs (`v0.7.2`) are validated, but **CLI vs Rust classification** can still drift without shared fixture trees.
- Teams want a **copy-paste CI recipe** for dry-run scans and JSON gates — not buried in milestone notes.

## Candidate scope

| ID | Deliverable | Status |
|----|-------------|--------|
| **P1** | **Shared classification fixtures** | Planned — `tests/fixtures/classification/` (or repo-root tree referenced by CLI + Rust) |
| **P2** | **Parity tests** | Planned — same path → same `kind` / risk tier in Vitest + `cargo test` |
| **A1** | **`docs/cli/ci-automation.md`** | Planned — dry-run, `--json`, exit codes, example reclaim threshold script |
| **D1** | **Docs hub links** | Planned — `docs/README.md`, [usage.md](../cli/usage.md) |

## Deferred (not v0.7.3)

| Item | Target band |
|------|-------------|
| README product demos (scan / cleanup / settings GIFs) | `v0.8.x` or `v1.x` — no official site; GitHub README remains the showcase surface when ready |
| Desktop policy-pack importer | Post-CLI validation (`v0.7.2` is CLI-first) |

## Non-goals

- README GIF capture or `docs/assets/demo/` content in 0.7.3  
- Scan contract `schema_version` bump without changelog + schema update  
- macOS/Linux GA installers (Phase E / `v0.8.x`)

## Verification

```bash
pnpm check
```

## References

- [version-roadmap.md](version-roadmap.md) Phase D — classification parity, automation  
- [scan-contract.md](../contract/scan-contract.md)  
- [CHANGELOG.md](../../CHANGELOG.md) — [Unreleased] / v0.7.3  
