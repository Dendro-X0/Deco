# v0.7.6 manifest — Community policy gallery

**Status:** Draft  
**Target release:** `v0.7.6`  
**Theme:** In-repo **policy gallery** — discoverable example packs and desktop polish for applying `.deco` policy (completes Phase D “shareable policy” backlog).

Parent: [v0.7.x-roadmap.md](v0.7.x-roadmap.md) · Follows [v0.7.5-manifest.md](v0.7.5-manifest.md).

---

## Problem

- Three examples under `examples/deco-policies/` are documented for CLI only; teams lack a guided gallery and apply UX beyond `v0.7.4` basics.
- Replacing an existing `.deco/disk-cleanup.json` has no merge/diff preview.

## Scope

| ID | Deliverable | Status |
|----|-------------|--------|
| **G1** | **Policy gallery** | Planned — index in `examples/deco-policies/`; optional 1–2 new packs; desktop browse/read-only catalog in Settings |
| **U1** | **Apply polish** | Planned — merge/replace preview when target `.deco` exists; reveal path in Explorer after apply |
| **D1** | **Docs** | Planned — [features.md](features.md), [user-guide.md](../desktop/user-guide.md), examples README |

## Non-goals

- Signed third-party marketplace (research only)  
- Changing review/blocked tiers via policy import  
- Workspace rollups (→ `v0.7.7`)

## Verification

```bash
pnpm check
```
