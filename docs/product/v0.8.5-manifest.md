# v0.8.5 manifest — Windows USN inventory (experimental)

**Status:** Shipped  
**Target release:** `v0.8.5`  
**Theme:** Optional **NTFS USN journal probe** on Windows (promotes [version-roadmap](version-roadmap.md) experimental track); non-Windows reports that the option does not apply. **Discovery remains the legacy directory walk.**

Parent: [v0.8.x-roadmap.md](v0.8.x-roadmap.md) · Follows [v0.8.4-manifest.md](v0.8.4-manifest.md).

---

## Scope

| ID | Deliverable | Status |
|----|-------------|--------|
| **E1** | **USN spike → setting** | Shipped — `experimental_windows_ntfs_usn_inventory` (default off); per-volume NTFS + `FSCTL_QUERY_USN_JOURNAL` probe; scan **warnings**; legacy walk unchanged |
| **T1** | **Correctness tests** | Shipped — `util::ntfs_usn` unit tests (drive-letter parsing; Windows smoke on `C:\` when present; non-Windows notice) |
| **D1** | **Docs** | Shipped — [windows-ntfs-usn-inventory.md](../experiments/windows-ntfs-usn-inventory.md); [scan-performance.md](../experiments/scan-performance.md) cross-link; Settings **Experimental** copy (`en`/`cn`/`es`) |

## Non-goals

- macOS/Linux journal dependency  
- Using USN for delete operations  
- Shortening discover wall time in v0.8.5 (future milestone)

## Verification

`pnpm check`; manual: enable setting on Windows, run scan, confirm scan **warnings** include USN line for NTFS volumes.
