# v0.7.7 manifest — Cleanup feedback & README demos

**Status:** Done on `main` — **not published** (tag `v0.7.7` predates green CI; ships in **`v0.7.8`** GitHub Release)  
**Target release:** ~~`v0.7.7`~~ → bundled into `v0.7.8`  
**Theme:** **Trust during cleanup** — live freed-space / folder progress, post-cleanup dashboard analysis, README GIF demos, and parallel-delete scheduling fix.

Parent: [v0.7.x-roadmap.md](v0.7.x-roadmap.md) · Follows [v0.7.6-manifest.md](v0.7.6-manifest.md).

---

## Problem

- Long `node_modules` deletes look like a hang; users cannot see bytes freed or folders completed.
- After cleanup, scan stats remain but there is no dedicated cleanup results summary.
- README lacked product demos; parallel chunked deletes ran smallest trees first (slow tail on large batches).

## Scope

| ID | Deliverable | Status |
|----|-------------|--------|
| **P1** | **Live cleanup progress** | Shipped — `freed_bytes_so_far` / `folders_done_so_far` on progress events; overlay + status |
| **P2** | **Cleanup results card** | Shipped — dashboard `CleanupStatisticsCard` after cleanup (like scan statistics) |
| **D1** | **README demos** | Shipped — `docs/assets/demo/*.gif` embedded in README |
| **F1** | **Delete ordering** | Shipped — largest trees first before chunking ([batch-delete.md](../experiments/batch-delete.md)) |

## Non-goals

- Workspace rollups (→ `v0.7.8`)  
- Scan contract bump

## Verification

```bash
pnpm install   # required after `pnpm clean` (removes node_modules)
pnpm check
pnpm dev:desktop
```

## References

- [batch-delete.md](../experiments/batch-delete.md)
