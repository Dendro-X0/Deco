# v0.7.8 manifest — Workspace rollups

**Status:** Shipped (`v0.7.8`)  
**Target release:** `v0.7.8` — **first GitHub Release after `v0.7.6`** (includes unpublished [v0.7.7](v0.7.7-manifest.md) work on `main`; no CI-only tag)  
**Theme:** **Monorepo workspace rollups** — summarize reclaim by project root without double-counting.

Parent: [v0.7.x-roadmap.md](v0.7.x-roadmap.md) · Builds on code from [v0.7.7-manifest.md](v0.7.7-manifest.md) (not separately released).

---

## Problem

- Large monorepos show flat candidate lists; users cannot see per-workspace or per-root totals before selecting cleanup.
- Double-counting parent/child paths undermines trust in planner totals.

## Scope

| ID | Deliverable | Status |
|----|-------------|--------|
| **W1** | **Rollup model** | Shipped — `buildWorkspaceRollups()` groups by `project_root`; per-risk subtotals; each artifact counted once |
| **U1** | **Dashboard UX** | Shipped — `WorkspaceRollupsCard` after scan statistics; per-workspace checkbox selects safe items only |
| **T1** | **Tests** | Shipped — `apps/cli/tests/workspace-rollups.test.ts` |
| **D1** | **Docs** | Shipped — user guide + features + changelog |

## Non-goals

- Auto-delete by rollup tier  
- Cross-machine sync of rollups  
- Scan contract bump unless wire shape requires documented bump

## Verification

```bash
pnpm check
```

## References

- [version-roadmap.md](version-roadmap.md) — Experimental track · Workspace rollups
